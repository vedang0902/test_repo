global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "demo_app"
    static_configs:
      - targets: ["host.docker.internal:8000"]

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "host.docker.internal:9093"

rule_files:
  - "alert.rules.yml"

